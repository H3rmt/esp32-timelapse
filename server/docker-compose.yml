services:
  esp32-timelapse-server:
    # image: h3rmt/esp32-timelapse-server
    build: .
    user: 1000:1000
    restart: always
    ports:
      - "8080:8080"
    environment:
      - LAYER_FPS=7
      - MINUTE_FPS=30
      - RUST_LOG=debug
    volumes:
      - ./imgs-c:/images
